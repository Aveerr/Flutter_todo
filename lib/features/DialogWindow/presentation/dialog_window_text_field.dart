import 'package:flutter/material.dart';

class DialogWindowTextField extends StatelessWidget {
  const DialogWindowTextField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
